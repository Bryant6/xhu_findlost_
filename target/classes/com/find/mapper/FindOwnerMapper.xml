<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.find.mapper.FindOwnerMapper">

    <select id="sortByPubtime" resultType="com.find.pojo.FindOwner">
        SELECT * from find_owner
        ORDER BY goods_pubtime
    </select>
    
    <select id = "selectData" resultType="String">
    	select now()
    </select>

</mapper>